{
    "files.exclude": {
        "node_modules": true,
        "out": true,
        "**/.git": true,
        "**/*.js": {
            "when": "$(basename).ts"
        },
        "**/*.js.map": {
            "when": "$(basename)"
        }
    }
}